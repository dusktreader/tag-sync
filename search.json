{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"Home","text":"<p>A CLI tool for syncing git tags with project versions.</p>","path":["Home"],"tags":[]},{"location":"#overview","level":2,"title":"Overview","text":"<p><code>tag-sync</code> keeps your git tags in step with your project's package version. It reads the current version from your project manifest (<code>pyproject.toml</code> for Python projects, <code>package.json</code> for npm projects), converts it to a canonical tag string, and pushes the new tag to origin. It also provides commands to verify, validate, publish, and remove tags. These are all wrapped up into a nice CLI.</p> <p>Key capabilities:</p> <ul> <li>Auto-detect the packager from the project directory</li> <li>Verify whether the current package version already has a published tag</li> <li>Check that an explicit tag string matches the current package version</li> <li>Publish a new tag derived from (or validated against) the package version</li> <li>Nuke a tag from both local and origin when you need to clean up</li> </ul>","path":["Home"],"tags":[]},{"location":"#why-tag-sync","level":2,"title":"Why tag-sync?","text":"<p>Manually keeping git tags aligned with <code>version = \"x.y.z\"</code> in your manifest is error-prone: you might forget to push the tag, push the wrong version, or end up with a published tag that doesn't match the package on PyPI.  <code>tag-sync</code> makes the manifest the single source of truth and enforces that contract at publish time.</p>","path":["Home"],"tags":[]},{"location":"features/","level":1,"title":"Features","text":"","path":["Features"],"tags":[]},{"location":"features/#packager-auto-detection","level":2,"title":"Packager auto-detection","text":"<p><code>tag-sync</code> inspects the target directory for known manifest files and selects the right packager automatically:</p> Manifest file Packager <code>pyproject.toml</code> <code>uv</code> <code>package.json</code> <code>npm</code> <p>If neither or both manifest files are present, <code>tag-sync</code> raises an error and asks you to pass <code>--packager</code> explicitly.</p> <p>Use <code>--packager</code> to override auto-detection at any time:</p> <pre><code>tag-sync verify --packager uv\ntag-sync verify --packager npm\n</code></pre>","path":["Features"],"tags":[]},{"location":"features/#directory-targeting","level":2,"title":"Directory targeting","text":"<p>All commands accept <code>--directory</code> / <code>-d</code> so you can operate on any project without changing your working directory:</p> <pre><code>tag-sync publish -d ~/projects/my-lib\n</code></pre> <p>The directory is passed to the packager for manifest lookup and used as the working directory for all git operations.</p>","path":["Features"],"tags":[]},{"location":"features/#tag-pattern","level":2,"title":"Tag pattern","text":"<p>Tags are formatted as <code>v&lt;major&gt;.&lt;minor&gt;.&lt;patch&gt;</code> by default (e.g. <code>v1.2.3</code>). The pattern is derived from the package version using SemVer parsing, so non-standard version strings are caught early with a clear error.</p>","path":["Features"],"tags":[]},{"location":"features/#verify-publication","level":2,"title":"Verify Publication","text":"<p>Reports whether the tag derived from the current package version is already published on origin.  No git state is modified.</p> <pre><code>tag-sync verify\n</code></pre>","path":["Features"],"tags":[]},{"location":"features/#check-that-a-tag-matches","level":2,"title":"Check that a tag matches","text":"<p>Validates that an explicit tag string matches the current package version. Useful as a pre-publish sanity check in CI or a Makefile.</p> <pre><code>tag-sync check v1.2.3\n</code></pre> <p>Exits with a non-zero code if the tag does not match.</p>","path":["Features"],"tags":[]},{"location":"features/#publish-a-new-tag-matching-the-project-version","level":2,"title":"Publish a new tag matching the project version","text":"<p>Creates a git tag and pushes it to origin.</p> <pre><code># Derive tag from package version (no version-match check)\ntag-sync publish\n\n# Supply tag explicitly (validates against package version first)\ntag-sync publish v1.2.3\n</code></pre>","path":["Features"],"tags":[]},{"location":"features/#already-published-guard","level":3,"title":"Already-published guard","text":"<p><code>tag-sync publish</code> always checks that the tag is not already on origin before pushing.  If it is, the command fails with a clear message.</p>","path":["Features"],"tags":[]},{"location":"features/#-replace","level":3,"title":"--replace","text":"<p>Pass <code>--replace</code> to overwrite an existing tag.  You will be prompted to confirm; the old tag is deleted locally and on origin before the new one is created.  If <code>--replace</code> is given but the tag is not yet published, the flag is silently ignored and the normal publish flow runs.</p> <pre><code>tag-sync publish --replace\n</code></pre>","path":["Features"],"tags":[]},{"location":"features/#-dry-run","level":3,"title":"--dry-run","text":"<p>Print what would happen without making any changes to local or remote state:</p> <pre><code>tag-sync publish --dry-run\n</code></pre>","path":["Features"],"tags":[]},{"location":"features/#nuke","level":2,"title":"nuke","text":"<p>Deletes a tag from both the local repository and origin.  Prompts for confirmation by default.</p> <pre><code>tag-sync nuke v1.2.3\n\n# Skip the prompt\ntag-sync nuke v1.2.3 --force\n</code></pre> <pre><code>tag-sync nuke v1.2.3 --dry-run\n</code></pre>","path":["Features"],"tags":[]},{"location":"publishing/","level":1,"title":"Publishing","text":"<p>This page covers how to set up a GitHub Actions workflow that automatically publishes your package to a registry whenever a <code>tag-sync</code> tag is pushed.</p> <ul> <li>Python packages — publish to PyPI with <code>uv publish</code></li> <li>npm packages — publish to npmjs.com with <code>npm publish</code></li> </ul>","path":["Publishing"],"tags":[]},{"location":"publishing/#how-the-workflow-fits-together","level":2,"title":"How the workflow fits together","text":"<p><code>tag-sync publish</code> creates and pushes the git tag.  GitHub Actions detects that push and runs the workflow, which:</p> <ol> <li>Verifies the pushed tag matches the current version in the manifest using    <code>tag-sync check</code> — a defence-in-depth guard against stale checkouts or    accidental tag pushes.</li> <li>Builds the package.</li> <li>Publishes to the registry.</li> </ol>","path":["Publishing"],"tags":[]},{"location":"publishing/#tag-pattern-matching","level":2,"title":"Tag pattern matching","text":"<p>All examples below use <code>v[0-9]+.[0-9]+.[0-9]+*</code> as the trigger pattern.  This matches the default <code>tag-sync</code> pattern for stable releases (<code>v1.2.3</code>) as well as pre-release suffixes (<code>v1.2.3a1</code>, <code>v1.2.3b2</code>, <code>v1.2.3rc1</code>), while rejecting arbitrary strings that merely start with <code>v</code>.</p> <p>If you use a custom tag pattern, adjust the filter accordingly:</p> <pre><code>on:\n  push:\n    tags:\n      - \"release/[0-9]+.[0-9]+.[0-9]+*\"\n</code></pre>","path":["Publishing"],"tags":[]},{"location":"publishing/#publishing-a-python-package-to-pypi","level":2,"title":"Publishing a Python package to PyPI","text":"","path":["Publishing"],"tags":[]},{"location":"publishing/#authentication-oidc-trusted-publishing","level":3,"title":"Authentication: OIDC trusted publishing","text":"<p>The recommended approach is OIDC trusted publishing.  PyPI issues a short-lived token directly to the workflow at runtime — no API tokens or secrets need to be stored in GitHub.</p>","path":["Publishing"],"tags":[]},{"location":"publishing/#one-time-pypi-setup","level":4,"title":"One-time PyPI setup","text":"<ol> <li>Go to your PyPI project → Manage → Publishing.</li> <li> <p>Under Add a new publisher, choose GitHub Actions and fill in:</p> Field Value Owner your GitHub username or org Repository your repo name Workflow filename <code>publish.yml</code> Environment name <code>pypi</code> (optional but recommended) </li> <li> <p>Click Add.</p> </li> </ol>","path":["Publishing"],"tags":[]},{"location":"publishing/#one-time-github-setup","level":4,"title":"One-time GitHub setup","text":"<p>In your repository go to Settings → Environments and create an environment named <code>pypi</code>.  You can add protection rules here — for example, requiring manual approval from a trusted reviewer before each publish run.</p>","path":["Publishing"],"tags":[]},{"location":"publishing/#workflow-file","level":3,"title":"Workflow file","text":"<p>Create <code>.github/workflows/publish.yml</code>:</p> <pre><code>name: Publish\n\non:\n  push:\n    tags:\n      - \"v[0-9]+.[0-9]+.[0-9]+*\"\n\njobs:\n  publish:\n    runs-on: ubuntu-latest\n    environment:\n      name: pypi\n      url: https://pypi.org/project/&lt;your-package&gt;/\n    permissions:\n      id-token: write   # required for OIDC trusted publishing\n      contents: read\n\n    steps:\n      - uses: actions/checkout@v4\n\n      - name: Install uv\n        uses: astral-sh/setup-uv@v7\n        with:\n          enable-cache: true\n\n      - name: Verify tag matches package version\n        run: uvx tag-sync check ${{ github.ref_name }}\n\n      - name: Build\n        run: uv build\n\n      - name: Publish\n        run: uv publish\n</code></pre>","path":["Publishing"],"tags":[]},{"location":"publishing/#running-tests-before-publishing","level":3,"title":"Running tests before publishing","text":"<p>Gate the publish on a passing test suite with a <code>needs:</code> dependency:</p> <pre><code>jobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: astral-sh/setup-uv@v7\n        with:\n          enable-cache: true\n      - run: uv sync --locked\n      - run: uv run pytest\n\n  publish:\n    needs: test\n    runs-on: ubuntu-latest\n    environment:\n      name: pypi\n      url: https://pypi.org/project/&lt;your-package&gt;/\n    permissions:\n      id-token: write\n      contents: read\n    steps:\n      - uses: actions/checkout@v4\n      - uses: astral-sh/setup-uv@v7\n        with:\n          enable-cache: true\n      - name: Verify tag matches package version\n        run: uvx tag-sync check ${{ github.ref_name }}\n      - run: uv build\n      - run: uv publish\n</code></pre>","path":["Publishing"],"tags":[]},{"location":"publishing/#end-to-end-flow","level":3,"title":"End-to-end flow","text":"<pre><code># 1. Bump the version in pyproject.toml\nuv version 1.3.0\n\n# 2. Commit the version bump\ngit add pyproject.toml uv.lock\ngit commit -m \"bump version to 1.3.0\"\n\n# 3. Push the tag — triggers the workflow\ntag-sync publish\n</code></pre>","path":["Publishing"],"tags":[]},{"location":"publishing/#publishing-an-npm-package-to-npmjscom","level":2,"title":"Publishing an npm package to npmjs.com","text":"","path":["Publishing"],"tags":[]},{"location":"publishing/#authentication-npm-access-token","level":3,"title":"Authentication: npm access token","text":"<p>npm does not yet support keyless OIDC publishing, so you need to store an access token as a GitHub Actions secret.</p>","path":["Publishing"],"tags":[]},{"location":"publishing/#one-time-npm-setup","level":4,"title":"One-time npm setup","text":"<ol> <li>Log in to npmjs.com and go to Access Tokens.</li> <li>Generate a new Automation token (scoped to publish).</li> <li>Copy the token.</li> </ol>","path":["Publishing"],"tags":[]},{"location":"publishing/#one-time-github-setup_1","level":4,"title":"One-time GitHub setup","text":"<p>In your repository go to Settings → Secrets and variables → Actions and add a secret named <code>NPM_TOKEN</code> with the token value.</p>","path":["Publishing"],"tags":[]},{"location":"publishing/#workflow-file_1","level":3,"title":"Workflow file","text":"<p>Create <code>.github/workflows/publish.yml</code>:</p> <pre><code>name: Publish\n\non:\n  push:\n    tags:\n      - \"v[0-9]+.[0-9]+.[0-9]+*\"\n\njobs:\n  publish:\n    runs-on: ubuntu-latest\n\n    steps:\n      - uses: actions/checkout@v4\n\n      - name: Set up Node\n        uses: actions/setup-node@v4\n        with:\n          node-version: \"lts/*\"\n          registry-url: \"https://registry.npmjs.org\"\n\n      - name: Install dependencies\n        run: npm ci\n\n      - name: Install uv\n        uses: astral-sh/setup-uv@v7\n\n      - name: Verify tag matches package version\n        run: uvx tag-sync check ${{ github.ref_name }}\n\n      - name: Publish\n        run: npm publish\n        env:\n          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}\n</code></pre> <p><code>actions/setup-node</code> writes an <code>.npmrc</code> that injects <code>NODE_AUTH_TOKEN</code> as the registry auth credential, so no manual <code>.npmrc</code> configuration is needed.</p>","path":["Publishing"],"tags":[]},{"location":"publishing/#running-tests-before-publishing_1","level":3,"title":"Running tests before publishing","text":"<pre><code>jobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: \"lts/*\"\n      - run: npm ci\n      - run: npm test\n\n  publish:\n    needs: test\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: \"lts/*\"\n          registry-url: \"https://registry.npmjs.org\"\n      - uses: astral-sh/setup-uv@v7\n      - run: npm ci\n      - name: Verify tag matches package version\n        run: uvx tag-sync check ${{ github.ref_name }}\n      - run: npm publish\n        env:\n          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}\n</code></pre>","path":["Publishing"],"tags":[]},{"location":"publishing/#end-to-end-flow_1","level":3,"title":"End-to-end flow","text":"<pre><code># 1. Bump the version in package.json\nnpm version 1.3.0 --no-git-tag-version\n\n# 2. Commit the version bump\ngit add package.json\ngit commit -m \"bump version to 1.3.0\"\n\n# 3. Push the tag — triggers the workflow\ntag-sync publish\n</code></pre>","path":["Publishing"],"tags":[]},{"location":"quickstart/","level":1,"title":"Quickstart","text":"","path":["Quickstart"],"tags":[]},{"location":"quickstart/#requirements","level":2,"title":"Requirements","text":"<ul> <li>Python 3.13+</li> <li>A project that uses <code>pyproject.toml</code> or <code>package.json</code> that includes version info.</li> <li>A git repository with a remote named <code>origin</code></li> </ul>","path":["Quickstart"],"tags":[]},{"location":"quickstart/#installation","level":2,"title":"Installation","text":"<p>Install from PyPI via pip or uv:</p> <pre><code>pip install tag-sync\n</code></pre> <pre><code>uv add tag-sync\n</code></pre>","path":["Quickstart"],"tags":[]},{"location":"quickstart/#basic-workflow","level":2,"title":"Basic workflow","text":"","path":["Quickstart"],"tags":[]},{"location":"quickstart/#check-whether-your-current-version-is-already-tagged","level":3,"title":"Check whether your current version is already tagged","text":"<pre><code>tag-sync verify\n</code></pre> <p><code>tag-sync</code> auto-detects your packager and reports whether the current version has a published tag on origin.</p>","path":["Quickstart"],"tags":[]},{"location":"quickstart/#publish-a-tag-for-the-current-version","level":3,"title":"Publish a tag for the current version","text":"<pre><code>tag-sync publish\n</code></pre> <p>This derives the tag from the package version, confirms the tag is not already on origin, creates it locally, and pushes it.</p>","path":["Quickstart"],"tags":[]},{"location":"quickstart/#validate-that-a-tag-matches-the-current-version","level":3,"title":"Validate that a tag matches the current version","text":"<pre><code>tag-sync check v1.2.3\n</code></pre> <p>Confirms that <code>v1.2.3</code> matches the version in your manifest. A tag argument is always required.</p>","path":["Quickstart"],"tags":[]},{"location":"quickstart/#publish-with-an-explicit-tag-with-version-validation","level":3,"title":"Publish with an explicit tag (with version validation)","text":"<pre><code>tag-sync publish v1.2.3\n</code></pre> <p>When a tag is supplied explicitly, <code>tag-sync</code> validates it against the manifest version before publishing.</p>","path":["Quickstart"],"tags":[]},{"location":"quickstart/#replace-an-already-published-tag","level":3,"title":"Replace an already-published tag","text":"<pre><code>tag-sync publish --replace\n</code></pre> <p>If the tag already exists on origin you will be prompted to confirm before the old tag is deleted and the new one is pushed.</p>","path":["Quickstart"],"tags":[]},{"location":"quickstart/#remove-a-tag-everywhere","level":3,"title":"Remove a tag everywhere","text":"<pre><code>tag-sync nuke v1.2.3\n</code></pre> <p>Deletes the tag from both the local repository and origin.  You will be prompted to confirm unless <code>--force</code> is passed.</p>","path":["Quickstart"],"tags":[]},{"location":"quickstart/#targeting-a-different-directory","level":2,"title":"Targeting a different directory","text":"<p>All commands accept <code>--directory</code> / <code>-d</code> to point at a project outside the current working directory:</p> <pre><code>tag-sync verify -d ~/projects/my-lib\ntag-sync publish -d ~/projects/my-lib\n</code></pre>","path":["Quickstart"],"tags":[]},{"location":"quickstart/#specifying-the-packager-explicitly","level":2,"title":"Specifying the packager explicitly","text":"<p>If your project root contains both <code>pyproject.toml</code> and <code>package.json</code>, auto-detection will raise an error and ask you to be explicit:</p> <pre><code>tag-sync verify --packager uv\ntag-sync verify --packager npm\n</code></pre>","path":["Quickstart"],"tags":[]}]}